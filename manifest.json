{
  "name": "Peronal Agent",
  "description": "Personal agent to repond to commands from UbiquityOS in user's Github actions",
  "ubiquity:listeners": ["issue_comment.created"],
  "configuration": {
    "default": {
      "configurableResponse": "Hello, world!"
    },
    "type": "object",
    "properties": {
      "configurableResponse": {
        "type": "string"
      },
      "customStringsUrl": {
        "type": "string"
      }
    },
    "required": ["configurableResponse"]
  }
}
